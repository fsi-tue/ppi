prevent sql injections

is max upload filesize working?

php.ini
; Maximum allowed size for uploaded files.
upload_max_filesize = 40M

; Must be greater than or equal to upload_max_filesize
post_max_size = 40M

prevent access to all files except php and images

add logging to all classes

collect all regularly executed tasks
        clean all download zip files
        remove borrowed records that are expired
        clean the debug log db

document all lib, dao and system functions also with params and return value (in javadoc style)



alle i18n strings schreiben

ins fsi github umziehen

Write Unit tests for all daos


cron jobs/cleanup jobs
system datenbank tabelle mit timings für cron jobs
clean up log table after x entries
clean up borrow records older than




mails besser formatieren (\r\n oder so)

